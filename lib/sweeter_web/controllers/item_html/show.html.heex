<div class="wrapper">
<div class="item-contents">
    <.item item={@item} restricted_tags={@restricted_tags} tags={@tags} item_load_count={@item_load_count} address={@address} />

    <div>Page Views<%= @item_load_count %></div>
</div>

<div id="item-reactions">
    <div id="your-reaction"></div>
    <div class="reactions">
    <ul> 
    <%= for reaction <- @item.reactions do %>
    <li><%= reaction.emoji %><span class="reaction-count"><%= reaction.count %></span></li>
    <% end %>
    </ul>
    </div>
    <div class="add-reactions">
    <div id="emoji-trigger">☀️your reactions🌑</div>
    <input type="hidden" id="item_id" value={@item.id}>
    <input type="hidden" id="address" value={@address}>
    </div>
    <%= if @item.search_suppressed == true do %>
    Search is suppressed, add 👍 and maybe it will be added to search results? (*not implemented)
    <% end %>
</div>
<div class="item-moderation">
    <%= if @is_moderator == true do %>
    As a moderator, you can help promote content by featuring it:<br />
        <%= if @item.featured == true do %>
        <a href={@unfeature_link} class="bg-teal-500 hover:bg-teal-700 text-white font-bold py-2 px-4 rounded">
        Remove from Featured
        </a>
        <%= else  %>
        <a href={@feature_link} class="bg-teal-500 hover:bg-teal-700 text-white font-bold py-2 px-4 rounded">
        Feature
        </a>
        <% end %>
    <% end %>

    <%= if @address != "" do %>
    <.moderation_form changeset={@moderation_changeset} action={~p"/moderations"} />
    <% end %>
</div>
</div>